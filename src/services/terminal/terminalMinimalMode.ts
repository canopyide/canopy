export const TERMINAL_MINIMAL_MODE: boolean = false;

// Granular toggles for trial-and-error debugging
export const TERMINAL_DISABLE_WEBGL: boolean = true;
export const TERMINAL_DISABLE_PARSER_FILTERS: boolean = false;
